---
description: REQUIRED — first substantive reply must include "### Active project rules" so the user sees which rules apply
alwaysApply: true
---

# State which project rules apply

Cursor injects rules from `.cursor/rules/ai-rules/`. Advertise which ones apply at the start of a chat so the user can verify.

## Mandatory output

In your **first substantive reply** of a new chat or fresh agent run, before any explanation, plan, or code, include this exact section. Copy it verbatim, then add glob-scoped or @-mentioned rows below if relevant.

```markdown
### Active project rules
- **`state-active-project-rules.mdc`** — announces active rules at the start of a chat.
- **`write-clean-code.mdc`** — readable, maintainable code defaults.
- **`reuse-code-before-duplicating.mdc`** — search and extract before duplicating.
- **`organize-repository-by-feature.mdc`** — feature-first folder layout.
- **`secure-code-data-and-dependencies.mdc`** — secure-by-default coding.
- **`prefer-lts-stable-runtimes-and-libraries.mdc`** — LTS / stable versions only.
- **`update-changelog-for-notable-changes.mdc`** — keep `CHANGELOG.md` current.
- **`append-and-deduplicate-requirements.mdc`** — capture and dedupe requirements.
- **`verify-syntax-and-fix-before-finishing.mdc`** — verify before finishing.
- **`remove-dead-code-and-unused-files.mdc`** — remove dead code safely.
- **`evolve-rules-when-codebase-patterns-change.mdc`** — evolve rules with the code.
```

## Add to the list when relevant

- **Glob-scoped rules** whose `globs` match the open files or task:
  - Markdown work → `use-this-format-for-markdown-files.mdc`
  - Editing `ai-rules` files → `write-cursor-rules-for-this-project.mdc`, `maintain-cursor-rules.mdc`
- **@-mentioned** rules.

## Skip a row only when

- That rule is disabled on disk (e.g. `*.mdc.disabled`) and no active `.mdc` exists.

## When to repeat

Print the section once per chat. Repeat when the user starts a clearly new topic, a fresh agent run, or asks which rules apply. Skip on tool-only or trivial follow-up turns.

You may put one short sentence (≤ 20 words) acknowledging the request **before** the heading, but nothing else above it.
